{
  "name": "hardhat-project",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ./ignition/modules/MemoryMapping.ts --network bscTestnet --verify",
    "verify": "hardhat ignition verify chain-97 --include-unrelated-contracts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.19"
  },
  "volta": {
    "node": "22.14.0"
  }
}
